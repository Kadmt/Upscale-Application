{
  "name": "upscale-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "copy-wasm": "node ./scripts/copy-wasm.js && node ./scripts/prepare-models.js",
    "dev": "npm run copy-wasm && vite",
    "build": "npm run copy-wasm && vite build",
    "preview": "vite preview",
    "get-model": "node ./scripts/download-model.js"
  },
  "dependencies": {
    "onnxruntime-web": "^1.15.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.0",
    "typescript": "^5.0.0",
    "vite": "^8.2.2"
  }
}
